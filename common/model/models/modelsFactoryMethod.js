import ModelCollections from './../enums/ModelCollections';

export default (collection) => {
    // Create the concrete model
    /* Add each model creation here */
    /* i.e. */
    /*
    if (collection === ModelCollections.proveedorCarga) {
      return new ProveedorCarga();
    } else if (collection === ModelCollections.user) {
      return new User();
    }
    */
};
